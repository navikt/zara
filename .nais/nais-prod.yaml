apiVersion: 'nais.io/v1alpha1'
kind: 'Application'
metadata:
  name: zara
  namespace: tsm
  labels:
    team: tsm
spec:
  port: 3000
  replicas:
    min: 2
    max: 2
  ingresses:
    - 'https://zara.intern.nav.no'
    - 'https://zara.ansatt.nav.no'
  leaderElection: true
  azure:
    application:
      enabled: true
      tenant: nav.no
      allowAllUsers: false
      claims:
        groups:
          # EntraAD: Team Symfoni
          - id: '74d71639-b6e1-487b-86e4-ca902a433f7c'
          # EntraAD: nais-team-tsm
          - id: '35c70ac7-e8e6-4cec-933b-86106a97c0d4'
          # EntraAD: Team Symfoni - Zara - Tilbakemeldinger
          - id: '95f6101c-0cd4-4d4a-9435-6b1a7775782c'
          # EntraAD: Team Symfoni - Zara - Bruksvilkår
          - id: '8510a25e-2c26-4088-a8ff-a4a2a9de3d21'
    sidecar:
      enabled: true
      autoLogin: true
      autoLoginIgnorePaths:
        - /api/*
        - /favicon.ico
  valkey:
    - instance: syk-inn
      access: readwrite
  postgres:
    clusterName: zara-postgres
  observability:
    autoInstrumentation:
      enabled: true
      runtime: nodejs
    logging:
      destinations:
        - id: loki
  prometheus:
    enabled: true
    path: /api/internal/metrics
  liveness:
    path: /api/internal/is_alive
  readiness:
    path: /api/internal/is_ready
  preStopHook:
    http:
      path: /api/internal/pre_stop
  filesFrom:
    - emptyDir:
        medium: Memory
      mountPath: /app/.next/cache
  accessPolicy:
    outbound:
      external:
        - host: cdn.nav.no
        - host: api.slack.com
      rules:
        - application: logging
          namespace: nais-system
  resources:
    requests:
      cpu: 50m
      memory: 200Mi
  envFrom:
    - secret: tsm-zara-secrets
