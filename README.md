# zara

Internal dev tool for tsm.

## Installation

Install the required deno version using `mise`

```bash
mise install
```

Install deps using deno:
```bash
deno install --allow-scripts=npm:@tailwindcss/oxide,npm:@parcel/watcher
```

Run the dev server:
```bash
deno run dev
```
